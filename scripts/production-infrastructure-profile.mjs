export const PRODUCTION_INFRASTRUCTURE_PROFILE_ID =
  "alibaba-hong-kong-single-host-seed-v1";

export const PRODUCTION_INFRASTRUCTURE_PROFILE = Object.freeze({
  id: PRODUCTION_INFRASTRUCTURE_PROFILE_ID,
  status: "selected-existing-not-converted",
  provider: "alibaba-cloud",
  region: Object.freeze({
    productionRegion: "china-hong-kong",
    status: "existing",
  }),
  applicationHost: Object.freeze({
    service: "simple-application-server",
    architecture: "linux-amd64",
    operatingSystem: "ubuntu-24.04-lts",
    existing: true,
    purchaseRequired: false,
    vcpu: 2,
    memoryGiB: 4,
    systemDiskGiB: 50,
    systemDiskClass: "essd",
    peakPublicBandwidthMbps: 200,
    highAvailability: false,
  }),
  postgresql: Object.freeze({
    service: "host-colocated-container",
    engineVersion: "17",
    authoritativeState: true,
    publicEndpoint: false,
    freshProductionStateRequired: true,
    stagingDataImportAllowed: false,
    requiredRpoMinutes: 60,
    requiredRtoMinutes: 240,
    encryptedOffHostRecoveryRequired: true,
    retention: Object.freeze({ daily: 14, weekly: 8, monthly: 12 }),
  }),
  queue: Object.freeze({
    service: "host-colocated-valkey-container",
    authoritativeState: false,
    publicEndpoint: false,
    freshProductionStateRequired: true,
    stagingDataImportAllowed: false,
    fixedQueueDepthLimit: null,
    fixedConcurrentJobLimit: null,
  }),
  objectStorage: Object.freeze({
    service: "cloudflare-r2",
    bucket: "goodgood",
    private: true,
    existing: true,
    stagingObjectImportAllowed: false,
    conversionRequirements: Object.freeze([
      "exact-object-inventory",
      "exact-deletion-dry-run",
      "separate-exact-target-approval",
      "empty-bucket-verification",
      "scoped-production-credential-rotation",
    ]),
  }),
  preproduction: Object.freeze({
    location: "operator-workstation",
    productionDataAllowed: false,
    productionSecretsAllowed: false,
    remoteStagingActive: false,
    reservedHostname: "staging-goodgood.o1key.com",
  }),
  resourceAdmission: Object.freeze({
    availableMemoryFloorMiB: 500,
    rootDiskUsageCeilingPercent: 80,
    cpuThreshold: null,
    fixedGenerationConcurrencyLimit: null,
    recovery: "operator-review-and-process-restart",
  }),
  conversion: Object.freeze({
    canonicalHostname: "goodgood.o1key.com",
    publicMode: "maintenance",
    maximumWindowMinutes: 240,
    cleanProductionStateRequired: true,
    stagingBusinessDataImportAllowed: false,
  }),
  authorization: Object.freeze({
    purchaseAuthorized: false,
    productionDeploymentAuthorized: false,
    executableReleaseAdapterAuthorized: false,
    liveConversionAuthorized: false,
    destructiveActionAuthorized: false,
  }),
});

export const PRODUCTION_SCALE_OUT_PROFILE = Object.freeze({
  id: "alibaba-managed-state-v1",
  status: "future-observation-triggered-option",
  provider: "alibaba-cloud",
  region: "china-hong-kong",
  applicationHost: Object.freeze({
    service: "ecs",
    architecture: "linux-amd64",
    minimumVcpu: 4,
    minimumMemoryGiB: 16,
    minimumSystemDiskGiB: 100,
  }),
  postgresql: Object.freeze({
    service: "apsaradb-rds-for-postgresql",
    engineVersion: "17",
    edition: "high-availability",
    privateEndpointOnly: true,
  }),
  queue: Object.freeze({
    service: "tair-redis-oss-compatible",
    architecture: "standard-master-replica",
    minimumMemoryGiB: 1,
    privateEndpointOnly: true,
    authoritativeState: false,
  }),
  authorization: Object.freeze({
    purchaseAuthorized: false,
    productionDeploymentAuthorized: false,
  }),
});
