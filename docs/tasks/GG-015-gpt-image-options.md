# GG-015 — GPT IMAGE 2 质量、背景与输出格式

- 状态：已上线；透明输出仍可继续人工验收
- 用户需求：GPT IMAGE 2 动态显示质量、背景和输出格式参数，并按 O1Key 图片生成契约传参
- 最后更新：2026-09-09
- 分支：`feature/GG-015-gpt-image-options`
- 依赖基线：本地组合候选 `8e4a13f`，包含 GG-004–GG-014；GPT IMAGE 2 尚未进入生产
- 决策：[ADR 0036](../decisions/0036-gpt-image-quality-background-format.md)

## 范围与验收

- 仅 GPT IMAGE 2 显示质量 `自动/低/中/高`、背景 `自动/透明`、输出格式
  `PNG/JPEG/WebP`；默认分别为自动、自动、JPEG。
- GPT 请求顶层显式传 `quality`、`background`、`output_format`；产品值与中文标签分离。
- 选择透明背景时 JPEG 不可用；若此前为 JPEG，则切为 PNG。服务端在计费和 provider
  POST 前再次拒绝透明 JPEG及所有非法/跨模型参数。
- 参数进入生成快照、哈希、草稿、项目、失败设置恢复、服务端重试和图片详情。
- JPEG/PNG/WebP 原始结果继续按解码 MIME 类型保存和下载，不做格式转换。
- GPT O1Key 路由版本升级为 v2；本地发布候选验证必须确认无旧 v1 活动任务。
- 保持每张 10 积分、现有尺寸/数量、Nano 参数和 `moderation` 不变。
- 自动化测试不得发起真实 provider 请求；不包含生产部署。

## 实现与证据

- 相关文件：生成共享契约、能力归一、快照、草稿/项目/批次持久化、O1Key 适配器、
  创作器与图片详情、迁移 0018、定向测试和专题文档。
- 已完成：共享契约、模型归一、不可变快照/哈希、草稿/项目/批次持久化、迁移 0018、
  O1Key GPT v2 映射、创作器联动、失败恢复、图片详情和专题文档均已实现。
- 验证：定向测试 70/70；最终 `npm run check:local` 共 234 项（228 通过、6 项
  opt-in 跳过、0 失败）。3010 Compose 全部健康并应用迁移 0018；数据库确认没有 queued/
  running 任务且没有活动 v1 attempt。默认格式修订为 JPEG 后，相关定向测试 45/45，
  完整门禁仍为 234 项（228 通过、6 项跳过、0 失败）；3010 Web/Worker 已重建且 readiness
  全部为 `ok`。真实 Chrome 刷新后验证从 Nano 切入 GPT 时 JPEG 选中、PNG/WebP 未选中；
  代理未点击生成。站长真实 `3:4 / 1K / 2 张 / 自动 / 自动 / JPEG` 任务成功并落库
  2 个 Asset，积分 `500 → 480`、冻结积分 0。
- 发布：随 `65ceb168` 上线；本次发布未新增 GPT 付费请求。

## 恢复工作

- 尚未完成：透明 PNG/WebP 与下载 MIME/透明通道仍可人工验收；生产发布已完成。
- 阻塞/风险：当前本地账户可用积分为 480、冻结积分为 0；发布时须先排空旧 GPT v1
  attempt（当前本地为 0）再切换 Worker。
- 下一步：站长可在生产按需验收透明 PNG/WebP；新的付费调用须另行明确授权。
