# GG-011 — 固化快速、安全的本地交付流程

- 状态：已完成（流程与文档契约，不需部署）
- 用户需求：总结 GG-010 的范围扩张和测试隔离失误，确保新窗口快速完成小修改且不再让测试任务进入真实 O1Key Worker
- 最后更新：2026-09-08
- 分支 / worktree：`chore/GG-011-fast-safe-local-delivery` / `F:/goodgood`
- 决策影响：不改变产品决定或线上行为，无需 ADR

## 范围与验收

- 根 `AGENTS.md` 必须直接要求最小行为修改、定向测试优先、完整门禁一次，以及真实 provider 测试隔离。
- `WORKFLOW.md` 给出可执行的快速顺序，避免小需求自动扩成重构、完整上线或反复构建。
- `TESTING.md` 明确 fixture 写入不能连接 3010 真实联调栈；真实生图可能计费，必须由站长明确发起。
- 文档契约测试锁定上述入口，后续删除关键规则会失败。

## 实现与证据

- GG-010 的必要复杂度来自 Nano 多图需要多个上游任务；可避免的延误来自过早扩大验证范围，以及 M6 opt-in 测试误用默认本地数据库。
- 测试事故已在 GG-010 留存具体证据；本任务只提炼可复用规则，不重复保存 task ID、凭据或用户数据。
- 未修改产品代码、数据库、Compose 或生产环境；不需要重建本地运行栈。
- `node --test tests/documentation-continuity.test.mjs`：8/8 通过；`git diff --check` 通过。

## 恢复工作

- 当前分支基于已提交的 GG-010 本地候选 `41d8e36`，因为本任务直接固化其复盘结论。
- 只需检查本任务文档 diff，运行 documentation-continuity 定向测试和 `git diff --check`。

## 下一步

- 后续新窗口按根 AGENTS 与 WORKFLOW 的快速顺序执行；如规则需要调整，保持入口、详细流程与契约测试同步。
