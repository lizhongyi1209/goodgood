# GG-050 — 移除页面右上角常驻返回入口

- 状态：本地完成并已获用户验收；完整门禁与原 Chrome 只读检查通过，未推送/合入/部署
- 更新：2026-09-13
- 分支 / worktree：`fix/GG-050-remove-page-return-actions` / `F:/goodgood-worktrees/GG-037`
- 基线：已验证 GG-049 `3641863`；main `bab17fd` 为祖先；保留未跟踪 `.codex/` 用户配置，不提交。
- 决策：[ADR 0061](../decisions/0061-remove-persistent-page-return-actions.md)，用户批准先移除，后续有需要再设计返回入口。

## 范围与验收

- 移除资产库（所有素材/展示模式）、积分记录、分销两个标签、企业四个标签、站长账户管理顶部右侧的常驻返回入口。
- 删除失去用途的回调参数、图标导入和按钮专用样式，不增加替代入口或改变页面主结构。
- 保留弹窗/图片视频详情关闭、错误区域恢复按钮、项目“新建创作”、退出登录、浏览器历史及企业历史创作上下文。
- 保持身份/权限、标签与筛选、创作状态、接口和持久数据不变。站长页仍独立，不顺带并入主界面。
- 定向 SSR/源码测试后稳定时一次完整本地门禁；仅更新原 32140 Web，复用原 Chrome 企业会话检查，不写模拟数据、不调用 provider。

## 证据与下一步

- 已只读排查并确认五类页面的返回入口与共享头部/样式；基线除 `.codex/` 外干净。
- 已删除五类头部入口、积分/分销返回回调接线、四个无用图标导入和两类按钮的桌面/响应式样式；模拟页沿用无返回的真实分销组件，保留原安全限制。
- 新增 5 项 GG-050 源码/SSR 测试，覆盖全部头部/无替代入口/无用接线、企业四标签加载和拒绝、积分加载、分销两个标签的加载/拒绝/填充/空态，以及关闭/错误恢复/新建创作/退出保留。GG-044/045/046/049/050 加文档定向 **43/43 通过**，无数据库/队列写入或 provider 任务。
- 稳定后一次 `npm run check:local` **通过**：lint、TypeScript、构建和 **417 项测试中 403 通过、14 opt-in 跳过、0 失败**；diff 检查通过。后续只更新交付文档，代码门禁未失效。
- 原 32140 loopback Web PID/命令核对后重启（新运行会话 `6165`），数据、会话和 Worker 保留。只读 readiness 五项 `ok`；视频 `available: true / persistence: false`，未提交 provider 任务。
- computer-use 复用原 Chrome 标签 `1648143727` 与 `enterprise-demo@local.goodgood` 企业会话：团队资产、积分记录、资产库均已显示无返回的头部，企业四标签和资产展示控件保留。最终恢复原团队资产 URL，截图并保留页面供检查；未自动登录/邀请/改身份/额度/关系。
- 验证边界：没有真实站长/分销商会话切换或窄屏浏览器实测；对应头部由源码/SSR 检查覆盖，不将企业浏览器证据当作所有身份的实测。
- 交付记录同步后文档连续性测试 **8/8 通过**、diff 检查通过；未跟踪 `.codex/` 用户配置不进入本地候选提交。
- 2026-09-13 用户确认并要求同步任务状态；实现提交 `903ad10`，仅追加本地文档收尾，不变更代码/价格、不重启服务或重新调用 provider。其余任务不自动补记为已验收或上线。
- 下一步：[GG-051 重新评估积分定价体系](GG-051-credit-pricing-reassessment.md) 待用户在新窗口启动，先评估方案再确认实施。当前候选仅保存在本地任务分支，不合入 main、不发布生产；返回入口后续有明确需求才设计。
