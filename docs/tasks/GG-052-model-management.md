# GG-052 — 模型管理与人民币积分定价

## 状态

本地实现与验证完成，模型管理页面已打开供用户试价；未推送、合入 main 或生产部署。

## 分支 / worktree

- `feature/GG-052-model-management`
- `F:/goodgood-worktrees/GG-052`
- 从 GG-051 `3b5c03d` 隔离建立，main 为已验证祖先；保留旧工作树、32140 服务与数据。

## 范围与验收

- 1 元 = 100 积分，旧资产购买力等值转换，不改不可变历史记录。
- 站长模型列表、添加现有接入模板的条目、编辑名称/说明、启用/禁用、人民币规格定价与积分试算。
- 图片目录及新任务服务端报价联动；权限、安全校验、版本冲突、价格快照和失败恢复。
- 视频按输出秒 + 参考视频秒配置/试算，明确现有预览不扣积分。
- 独立 mock 本地栈供页面验收；不创建真实 provider 请求。

## 决策影响

[ADR 0063](../decisions/0063-model-management-and-cent-credits.md) 替代旧单位数字，明确动态目录及现有模板的添加边界。

## 实现与证据

- 已检查目录、不可变价格/积分账本、账户管理、图片任务及视频预览边界。
- 已实现单位迁移、动态目录/价格发布、站长面板、图片提交报价版本保护、草稿/项目目录 ID 保留与旧任务退款换算。
- 独立 mock 栈迁移到 0030，站长通过正常本地认证进入 32141/admin/models。新增 `banana-pricing-demo` 的 1K/2K/4K 售价为 0.21/0.32/0.53 元，启禁 v1→v2→v3；仅该独立栈的验收条目。
- Chrome 已验证刷新后新增仍存在，启用/禁用保存；图片 0.25 元×4=100 积分、视频 1.23 元×5 输出秒+0.45 元×3 参考秒=750 积分，示例视频价格未保存。
- 新模型单元测试 10/10，定向模型/活动/发布/文档 28/28。独立无 Worker PostgreSQL `goodgood_gg052_pricing_test_v2` 实测 1/1：个人 90→180、充值来源 80→160、企业 490→980/已分配 190→380、成员额度 200→400/已用 10→20，旧账本不变；原自定义 25 旧积分→50 新积分在面板初始化保留；旧任务退款、报价过期、原价结算、禁用后已受理 claim 通过。
- 稳定后完整 `npm run check:local` 首次代码检查/构建通过，唯一历史发布文档契约因计划精简失败；补回历史域名/CURRENT_STATE 链接后 15/15 复验。浏览器发现模型顺序改变，修复为已确认模板顺序并添加回归断言，因此最终完整门禁复跑：lint/TypeScript/构建通过，428 项中 413 通过、15 opt-in 跳过、0 失败。
- Chrome 完成新增模型选取/21 积分报价、一次 mock 生成、成品命名、项目保存和刷新恢复；任务 `54df663c-00d7-48c1-9c09-4ab5f6f41cd8` succeeded，目录 `banana-pricing-demo`/报价 21 新积分；余额 200→179、预留 0。项目 `3a5286ea-1aac-4976-be2e-935f09342a5f` 保留目录 ID 和名称。
- 390×844 定价弹窗核对无横向溢出，内部滚动后试算/启用/保存可达，未修改初始价格；视口恢复。最终 Chrome 模型管理页已标记保留，无未保存示例输入。
- 最终 Web 会话 `79354`、Worker `99942`、mock provider `68846`；Web/Worker readiness 五项均 ok。没有真实 provider 请求、邮件或生产访问。

## 恢复工作

保留旧单元和历史记录；转换必须无活动任务、预留及待支付订单。测试写入仅命名的独立 mock 栈，不能连接 32140 的真实 provider Worker。

- 本地 Compose `goodgood-gg052`；数据库 55449、Valkey 56449、对象存储 59049/59050，Web 32141、Worker 32142、mock 32143；原 32140 不重启、不换数据。
- Ignored `.gg052-local.mjs` 启动 web/worker/mock-generation；Web 使用 ignored `server/runtime/.gg052-web.mjs` 同步本机实际 `os.freemem()` 和 F 盘 `statfs` 到原资源保护器（仍是 500 MiB/80% 阈值）。最初 Windows 无 `/proc/meminfo` 触发保护且没有扣费，正常资源观测后重启本任务 Web并重试成功；生产 Linux 探针没有修改。
- 用 `node .gg052-local.mjs web` 恢复 Web；构建后须重新启动避免旧模块缓存。数据库验收条目和 mock 资产保留供用户查看，不写入 Git。

## 下一步

用户在 `http://127.0.0.1:32141/admin/models` 点击编辑/定价尝试人民币费率；新增条目复用已有模板。正式视频结算、新上游协议及生产单位转换另行确定范围，当前不发布。
