# GG-060 — 站长管理标题层级

## 状态与基线

2026-09-13：本地视觉细化、完整门禁与原页面验证完成，未发布。分支 `fix/GG-060-management-heading-hierarchy`，
worktree `F:/goodgood-worktrees/GG-060`，接续 GG-059 `68cf479`；main 已核验为祖先。

## 范围与决定

- 用户指出右侧上下两个相似标题缺少主次；检查共享页头和实际页面标题。
- 侧栏已提供站长管理上下文，右侧移除重复站长标题，只显示三个功能切换。
  当前企业/模型/账户页面的 20px 标题为主标题，描述保持较轻的次级文字。
- 保留导航的可访问名称和当前项、40px 点击区域、焦点与窄屏换行；
  移除不再使用的标题布局样式，不加分隔线或强面板。
- 这是 ADR 0067 的视觉细化，更新该 ADR，不改变已确认路由、工作区与权限。
- 只更新 32141 local/mock Web，不改价/账户/历史，不迁移、生成、推送或部署；
  原工作树、32140 真实 provider 栈及正式生产保留。

## 验证

- 既有管理/页头/访问门禁定向 11/11 通过；该低风险视觉修改不新增镜像实现的测试。
- 稳定后一次 `npm run check:local`（4940）完成：453 项中 437 通过/
  16 opt-in 跳过/0 失败，lint/TypeScript/构建全部通过；日志 ignored
  `.gg052-gg060-check.log`，之后没有运行代码修改。
- 停止 GG-059 Web 82667，确认 32141 无监听后启动 GG-060 Web 43528；
  未变的 Worker 36299/mock 62817 仍来自 GG-057，三角色 ready/checks 全 ok。
  独立 `goodgood-gg052` 配置保留，没有迁移或新 SQL fixture。
- Chrome 原站长标签 1648144149：三页面右侧顶部都只有三个切换项，当前页
  各只有一个 h1；模型主标题为 20px/600，中文字体设置保留。桌面截图清楚
  显示导航、当前主标题、次级说明的关系；原侧栏/添加模型/定价功能保留。
- 390×844 截图检查：切换项 12px、点击区 40px，模型页主标题与说明层级
  正常，未出现右侧重复站长标题。临时 viewport 已恢复，最后模型页标记保留。
- 模型全记录与九类历史 count/hash 前后精确一致：管理事件 12/价格版本
  120/积分流水 4/积分账户 2/批次、任务、尝试、资产、项目各 1。copied helper
  沿用 ignored `.gg052-gg057-before.json` / `after.json` 名称，位于 GG-060。
- 最终文档连续性/发布边界 15/15 与 diff 检查通过；没有秘密、数据库、截图、
  日志、本地 helper 或用户素材进入 diff，不作 Windows 栅格化保证。

## 下一步

用户在 `http://127.0.0.1:32141/admin/models` 查看标题层级，并切换另外两页。
本地实现与验证完成，价格/账户/历史保留；正式生产和 32140 不变，没有
推送、合 main、部署或真实付费请求。
