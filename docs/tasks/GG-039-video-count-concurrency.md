# GG-039 — 视频 1 / 2 / 4 数量与并发

- 状态：本地完成并验证，待用户检查
- 最后更新：2026-09-13
- 分支 / 目录：`feature/GG-039-video-count-concurrency` / `F:/goodgood-worktrees/GG-037`
- 基线：GG-038 已验证 `ed637e7`；同目录顺序新分支，依赖候选堆叠，未合入 main
- 决策：[ADR 0052](../decisions/0052-video-count-and-concurrent-runs.md)

## 范围与验收

- 视频数量 1/2/4，默认 1；切换模式/模型/线路不重置，图片数量独立。
- 一次提交并发启动对应数量的单视频请求；已在生成时允许新批次。
- 冻结每次输入，稳定独立槽位、状态/成功/失败隔离；不向上游传 count/n。
- 查询中断保留任务 ID，仅恢复查询，不自动重新提交计费请求。
- 沿用已确认紧凑卡片尺寸展示本地任务；不扩展持久化、定价、素材上传或生产入口。
- 定向并发/失败/乱序测试、完整本地门禁、现有 Chrome UI；不进行真实 provider 请求。

## 恢复

- 已实现：数量控件、冻结输入、独立并发 run、紧凑混排卡片、本地视频详情与查询恢复。
- `npm run check:local` 通过：lint、TypeScript、本地构建；360 项测试，346 通过、14 个 opt-in 跳过、0 失败。首次 lint 与两项旧源码断言失败已修正后复验。
- GG-039 的 5 项 stub 测试覆盖 1/2/4 槽位、4 个同时启动的 POST 与各自 GET、追加批次、乱序完成、单项失败、未知提交和已知任务的查询恢复；没有 count/n 上游字段或真实 provider 请求。
- 现有 Chrome extension 标签 `http://127.0.0.1:32139/create?media-preview=1` 经 computer use 验证默认 1、选择 2/4、图片数量独立及图片/视频/模型/线路/生成模式切换保持；未使用 Playwright。已保留视频参数展开、标准、多模态、Seedance 2.5、数量 4。
- 新本地开发进程仅监听 `127.0.0.1:32139`，没有启用临时 key 或真实接口；页面仍标明样式模拟，不伪造真实成功。
- 下一步：用户检查数量布局；如需真实并发实测，另行授权启用该分支本地接口并确认潜在 2/4 次生成费用。素材、正式资产/项目持久化和计价仍未接入，本地视频详情只遍历本地视频结果。
- 未推送、未合入、未部署；原 32138 用户真实视频页保持不变。
