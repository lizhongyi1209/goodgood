# GG-043 — 放大素材缩略图与点击预览

- 状态：本地实现与自动化完成；用户已完成页面检查，未发布
- 更新：2026-09-13
- 分支/目录：`feature/GG-043-larger-reference-previews` / `F:/goodgood-worktrees/GG-037`
- 基线：已验证 GG-042 `8c7a6d8`，干净依赖候选上顺序新分支；main/其他 worktree 不变。
- 决策：[ADR 0056](../decisions/0056-larger-reference-previews.md)

## 验收与检查点

- 图片/视频共用 96px 桌面、80px 手机的 1:1 缩略图；横向溢出与标签/数量限制不变。
- 图片继续点击进入完整大图编辑；视频模式图片/视频/音频点击或 Enter/Space 只读预览。
- 预览完整图片、手动控制视频/音频；加载/失败重试与关闭不丢素材，不误触移除或提交。
- 参数抽屉仍贴边覆盖；新托盘高度变动后视口限制仍有效。
- 已实现：共用尺寸 token 96px/80px；图片现有大图编辑不变；视频模式独立原生预览按钮（键盘 Enter/Space）、Radix 只读媒体弹窗、加载/失败重试和关闭焦点归还。移除按钮为独立兄弟控件，不打开预览。
- 定向测试 39/39；最新 `npm run check:local` 通过：374 项中 360 通过、14 个 opt-in 跳过、0 失败，lint 无警告、TypeScript 与构建通过。首次门禁发现 group 的 aria-haspopup 警告后改成独立原生按钮并重跑完整门禁。
- 现有 Chrome computer use 已打开资产选择器确认本地没有素材，并重新加载 32139 测试页。没有上传文件；已异步请求合成测试素材上传许可，未收到答复，因此不将空页面检查当作真实素材放大/播放或窄屏预览证据。
- 用户反馈：2026-09-13 表示“检查完毕”，随后要求启动真实接口版进行站长全排查；不把用户检查扩写为代理已执行素材播放/窄屏测试。
- 下一步：在最新候选 32140 真实接口本地入口进行站长功能全排查，运行记录见 GG-036；未发布。
- 不提交真实生成、创建素材或写 fixture，不触碰 32138，不推送/合入/部署。
