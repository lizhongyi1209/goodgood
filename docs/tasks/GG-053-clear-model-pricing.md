# GG-053 — 精简模型列表与规格价格布局

## 状态

本地完成并验证；原定价页已更新供用户检查，未发布。

## 分支 / worktree

- `fix/GG-053-clear-model-pricing`
- `F:/goodgood-worktrees/GG-053`
- 接续已交付 GG-052 `7d75525`，main `bab17fd` 为已验证祖先；旧工作树与独立 mock 栈数据保留。

## 范围与验收

- 列表模型名称只出现一次，移除重复内部 ID、接入模板和版本号。
- 图片/视频分组，按分辨率对齐价格；人民币主值和积分换算清楚，视频输出/参考秒价格分别显示，未定价与不支持区分。
- 编辑优先名称/售价；内部编号自动生成，已有编号保留，接入细节按需展开并解释用途。
- 保留现有添加/编辑/启禁、报价及冲突恢复；不改变积分单位/扣费规则/接入协议，不新增迁移。
- 更新原 Chrome 定价页，桌面/窄屏和试算检查；不调用真实 provider。

## 决策影响

不改变 ADR 0063 的计价、稳定目录 ID 或接入边界；仅优化信息展示，无需新 ADR。

## 实现与证据

- 已恢复 GG-052 代码/文档和验收栈；冗杂来源为列表同时显示 name、id、adapterId 和 vN，价格拼为一行。
- 已实现 `model-pricing-list.tsx` 图片/视频分组与规格列、人民币/积分层级、未定价/不支持区分；列表没有编号/模板/vN。编辑价格按规格分组，接入详情默认折叠，新增编号使用 UUID 自动生成，已有编号不变。
- 定向 lint、TypeScript 通过；原模型管理契约 10 项与文档连续性 8 项全部通过。
- 稳定代码后一次 `npm run check:local` 通过：lint、TypeScript、构建及 428 项测试，413 通过、15 opt-in 跳过、0 失败（会话 `30651`，本地 ignored `.gg052-check.log`）。
- 原 Chrome `32141/admin/models` 已验证名称一次、图片/视频筛选、分辨率价格对齐、未定价/不支持区分；编辑默认隐藏内部细节，新增自动编号可展开查看后取消。桌面与 390×844 图片/视频列表及编辑框无横向溢出，底部取消/保存可滚动到达，视口与全部模型筛选已恢复。
- 未保存试算：图片 1K 每张 ¥0.25 × 4 = ¥1.00 / 100 积分；视频 4K 输出 ¥1.23/秒 × 5 秒 + 参考 ¥0.45/秒 × 3 秒 = ¥7.50 / 750 积分。均取消，原试价数据、启禁与配置版本不变；本次没有生成请求或真实 provider 请求。
- 只从新工作树重启 Web `32141`，会话 `43172`；原 Worker `99942` 与 mock `68846` 保留，Web/Worker readiness 五项均为 ok。页面已标记保留。
- 最终文档连续性与生产发布边界测试 15/15 通过，`git diff --check` 通过；交付只含 UI 和相关文档，ignored 本地启动入口、日志及数据不入提交。

## 恢复工作

原 mock 栈 `goodgood-gg052`、数据库 55449/Valkey 56449/存储 59049 保留；只更新 Web 32141，不动真实 32140 或 Worker，不执行转换迁移。

## 下一步

用户在 `http://127.0.0.1:32141/admin/models` 检查布局并继续试定价格；本地独立栈保留，不推送、不合 main、不部署。生产转换与正式视频结算仍按 GG-052 的后续范围处理。
