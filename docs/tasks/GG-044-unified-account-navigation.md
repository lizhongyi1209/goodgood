# GG-044 — 个人账户主导航与企业管理布局

- 状态：本地实现、自动化及企业账户桌面检查完成；待用户验收，未发布
- 更新：2026-09-13
- 分支/目录：`feature/GG-044-unified-account-navigation` / `F:/goodgood-worktrees/GG-037`
- 基线：核验 main `bab17fd` 为当前候选祖先；从干净且已验收的顺序依赖候选 `589913d` 新建隔离分支，保留 GG-032—043，不合并 C6。
- 决策：[ADR 0057](../decisions/0057-personal-account-main-navigation.md)

## 范围与验收

- 站长、企业和普通账户均移除桌面/手机工作区切换。
- 企业管理与积分分配在左侧主功能栏；入口可见性不代替服务端权限。
- 企业管理沿用主界面，内部横向概览/成员与额度/消费记录/团队资产，不新增企业侧栏。
- 遵循 DESIGN_SYSTEM：紧凑白色工作界面、20px 标题、中性普通操作、移动端可读字段、统一弹框。
- 个人创作/轮询不因管理导航而清空；保留历史企业 URL 和权限验证，数据与积分归属不变。
- 不做迁移、真实生成、fixture 写入、生产访问、推送或发布。

## 当前检查点

- 已实现：无 UI 的目录 hook 保留历史企业权限校验；所有身份移除全局选择器；企业管理与积分分配进入主侧栏/移动入口；组织路由复用主 shell，创作/轮询不卸载；单企业直达概览，多企业管理列表与邀请接受不会切换创作身份。
- 企业布局：20px 标题、紧凑横向标签、白色摘要/管理入口；桌面成员表明确累计/消费/预留/剩余，900px 以下逐项卡片；消费/资产空态、成员状态、原比例团队资产、中性操作及统一邀请/额度弹框。失败保留输入，旧读取不覆盖新标签。
- 定向首轮 40 项中 38 通过，发现 JSX 多余括号后修正；GG-044 重测 6/6。最终完整 `npm run check:local` 一次通过：380 项中 366 通过、14 项 opt-in 跳过、0 失败；lint、TypeScript、构建通过。未启用数据库/队列写测试。
- 已重启 32140 最新构建，沿用隔离真实栈/数据库/对象/Worker 与企业会话，不重建容器。只控制现有 Chrome 1648143727，已检查新主侧栏、概览、成员表、邀请与额度弹框；打开后取消，没有创建邀请、改角色/额度、上传或提交生成。
- `/api/health/ready` 为 ready，database/objectStorage/provider/queue/runtime 均 ok；文档连续性 8/8 与 `git diff --check` 通过。测试标签停在企业成员页面并保留供检查，服务执行会话 95458。
- 浏览器实际会话为 `enterprise-demo@local.goodgood`；站长/普通入口规则由源码/测试覆盖，本轮未切换登录。响应式字段有自动化契约覆盖，本轮未做实际窄屏截图或预算 mutation，不扩写为新端到端企业/积分验收。
- 参考 Sites 既有页面修改规范保持架构/组件；无注册、发布或托管改变。CURRENT_STATE 生产事实不变。
- 下一步：用户在 32140 现有企业测试账户检查主导航/管理样式，再决定后续功能；无 blocker，不合入/推送/部署。
