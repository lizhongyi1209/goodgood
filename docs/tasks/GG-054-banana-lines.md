# GG-054 — Banana 三线路与分线路定价

## 状态

Pro 三线路和通用选线/定价功能本地实现并验证，页面已保留；当时 Nano Banana 2 优质/专线请求 ID 待补齐。用户随后在 [GG-056](GG-056-banana2-lines.md) 提供映射并接续实现；当前状态以该任务为准。未推送/合 main/部署或发真实付费请求。

## 分支 / worktree

- `feature/GG-054-banana-lines`，`F:/goodgood-worktrees/GG-054`
- 接续已交付 GG-053 `adcfc67`，main `bab17fd` 已验证为祖先；原工作树和真实 32140 栈保留。

## 范围与验收

- Banana 系列一个模型保留三条产品线路：`special / quality / dedicated`，中文特价/优质/专线，默认特价。
- Pro 服务端请求 ID：`gemini-3-pro-image-c-sp` / `gemini-3-pro-image-c-sd` / `gemini-3-pro-image`。
- Nano Banana 2 原特价 `gemini-3.1-flash-image-c-sp`；另两条映射已向用户询问，不猜测并发起请求。
- 站长按模型/线路/1K、2K、4K 设置人民币每张售价及线路启禁；一个模型名称，下面三条价格行。
- 创作者可以选已启用且定价完整的线路；报价、预留、个人/企业结算均按该线路。受理后线路和价格不变，不自动回退。
- 草稿、项目、批次和历史详情保留所选线路；旧数据仍恢复特价，不重写旧报价/账本。
- 无真实 provider 请求；以独立 disposable SQL 数据库和 mock 浏览器流程验证，稳定后一次完整门禁。

## 决策影响

新增 [ADR 0064](../decisions/0064-banana-lines-and-specification-prices.md)，扩展固定单线路和 Pro 未开放边界；延续人民币每张固定售价和 1 元/100 积分，不以专线实际 token 浮动追扣。

## 实现与证据

- 新增产品线路契约、服务端 Pro 精确映射与线路独立价格作用域；保持原 Banana 2 特价 route/version、多图一张一任务的持久提交与整批结算；Pro 单张、十种比例和三种分辨率。
- 分线路启禁和完整价格校验在积分预留前执行，接受后批次线路/不可变报价贯穿个人和企业结算。改价/禁用不影响已受理任务，不自动换线。默认特价与旧省略值 hash 保持一致。
- 创作器默认特价，切线报价匹配独立作用域；草稿/项目/重试/详情保留所选线路。模型列表名称一次、三条价格行；编辑切线保留未保存输入，按人民币展示并自动换算积分。
- 迁移 `0031_gg054_banana_lines.sql` 只加线路配置与状态列，继承旧价格到特价，另两条禁用/空价格，保留旧配置版本/时间、hash、报价与账本。独立 mock 库已执行 0031，生产仍为 0019。
- 定向 47/47 通过；九个新增线路单测含 90 组注入 fetch 的 Pro ID/比例/分辨率请求校验，不发 HTTP。前端契约验证线路快照/恢复、Pro 数量/比例及切线报价/缺价恢复。
- 独立无 Worker `goodgood_gg054_lines_test_v2` SQL 1/1 通过：迁移历史不变、不同线路幂等冲突、改价/禁用拒绝新请求、42→55 改价后仍按 42 结算、重复成功回调只扣一次；企业专线 95 结算、特价 30 失败重复释放、个人余额不变、草稿/项目线路落库。库无附着队列/provider，完成后活动任务为 0。首次 fixture 将批次数据库 ID 当作 API 的 batchIds（实际 job ID），已修正并在新 v2 数据库重跑。
- `npm run check:local` 最终 lint/TypeScript/构建通过；439 项中 423 通过、16 opt-in 跳过、0 失败。首轮仅旧 CI 迁移预期与恢复说明缺历史域名两项失败，更新预期/说明后定向 11/11，再完整复验通过；日志为 ignored `.gg052-gg054-check-final.log`。
- Chrome 原 32141 页桌面/390×844 检查：价格矩阵对齐，Pro 优质 0.42/0.52/0.72 元显示 42/52/72 积分；专线输入与优质独立，往返切换保留输入，窄屏保存/取消可达。试算全部取消，未保存费率。创作器默认特价，未启用线路禁用；视口已恢复，模型管理 tab 已标记保留。
- 本地三角色已更新至 GG-054：Web 会话 `3926`、mock Worker `43261`、mock provider `29023`，Web/Worker readiness 五项 ok。ignored `.gg052-local.mjs`、`server/runtime/.gg052-web.mjs` 使用显式独立 loopback mock 配置；Windows 实际资源探针保持原保护阈值。
- 旧迁移在新 checkout 的文本字节不同；核对 GG-052 已执行文件 checksum 与数据库完全匹配、标准化文本内容与本 checkout 一致后，仅用独立 0031 目录追加迁移。没有改旧 migration checksum 或执行转换/reset。
- 用户试价条目 `banana-pricing-demo` 21/32/53 积分、v3 与原模型价格/启禁不变，个人余额 179/预留 0，项目「定价联动验收（本地）」v1 保留。未发生成请求。旧真实 32140 栈及生产事实不变。
- 最终交接文档连续性/发布边界定向 15/15 通过，diff 检查通过；无密钥、环境文件、日志、构建产物或真实资产进入提交。

## 下一步

用户已在 [本地模型管理页](http://127.0.0.1:32141/admin/models) 保存 Banana 价格，并补齐 Banana 2 映射；后续见 GG-056，保留已保存价格并移除试价目录条目。生产发布/真实 provider 冒烟、正式视频扣费另行安排。
