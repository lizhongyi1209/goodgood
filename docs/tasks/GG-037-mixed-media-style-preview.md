# GG-037 — 图片与视频混排样式模拟

- 状态：本地样式模拟完成，待用户确认；未发布
- 最后更新：2026-09-13
- 分支 / worktree：`feature/GG-037-mixed-media-style-preview` / `F:/goodgood-worktrees/GG-037`
- 基线：GG-036 已验证候选 `1aaf0c2`；堆叠依赖，不代表 main 或生产
- 决策：[ADR 0051](../decisions/0051-mixed-media-style-preview.md)

## 范围与验收

- 用假数据模拟图片/视频混排；沿用图片四列/窄屏两列、真实比例、紧密间距。
- 排队、生成中、失败视频只占卡片位置；完成视频用封面、播放标识、时长。
- 点击图片/视频进入同一种三分区详情，可通过缩略轨道、方向键与滚轮切换。
- 明确标注模拟；不创建真实任务、不改接口参数、计费、数据库、资产或项目记录。
- 独立预览专页，不重新加载用户 GG-036 已生成视频页；现有 Chrome computer use，不用 Playwright。

## 实现与验证

- 已创建隔离工作树，保留其他任务与用户页面。
- 已实现独立 feature 组件、preview-session + `?media-preview=1` 双条件入口；不写入现有创作记录。
- 模拟包含 3 张图片、2 个完成视频、排队/生成中/失败各一个视频；沿用现有 masonry/detail 类。
- 定向测试 2/2 通过；完整本地门禁 354 项中 340 通过、14 个 opt-in 跳过、0 失败。
- 现有 Chrome computer use 已检查四列网格、横向/竖向视频详情、混合缩略轨道、方向键、滚轮、
  模拟播放和 Escape 关闭；未使用 Playwright。窄屏两列渲染已覆盖，实际窄屏浏览器复核待用户检查。
- 专用预览页：`http://127.0.0.1:32139/create?media-preview=1`；Vite 服务保留，未启用 Seedance key/真实接口。
- 初载与生产构建并行导致开发模块缓存失效；完成门禁后重启 dev 服务恢复。原 32138 真实视频页未操作。
- 未推送、未合入、未部署。

## 下一步

用户确认卡片尺度、播放/时长标识和统一详情；正式混合媒体持久化需另行范围确认。
