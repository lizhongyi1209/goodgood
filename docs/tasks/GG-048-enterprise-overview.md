# GG-048 — 面向企业管理员的概览

- 状态：本地页面与验证完成，原 Chrome 已展示；待用户确认布局，未推送/合入/部署
- 更新：2026-09-13
- 分支/目录：`feature/GG-048-enterprise-overview` / `F:/goodgood-worktrees/GG-037`
- 基线：已验证 GG-046 `93ce764`（源码门禁对应 `3080ae9`），main `bab17fd` 为祖先；GG-047 已由另一工作树占用。原未跟踪 `.codex/` 为用户配置，保留且不提交。
- 决策：[ADR 0059](../decisions/0059-actionable-enterprise-overview.md)。替换概览重复快捷入口，权限、余额、账本和其他管理导航不变。

## 范围与验收

- 直接更新真实企业账户的概览，不新建模拟身份或注入假账本。
- 四个核心指标、需要关注、成员使用概况、最近团队成品；轻量白色布局、响应式、键盘可达。
- 真实余额/未分配额度及成员累计消费/剩余额度使用已有 dashboard；周期指标标注待接入，不能把有限记录相加冒充本月总数。
- 零积分、零/无成员额度、即将过期邀请是可定位的提示；调整额度复用现有确认弹框，不自动写入。
- 最近成品读取既有企业资产边界，局部加载/空态/失败/重试，不读取个人或原始参考素材；支持只读放大查看。
- 不接新的统计后端、定价、视频持久化或生产发布，不创建账户/邀请/关系/积分/任务。
- 定向测试，稳定后一次 `npm run check:local`，原 32140 最新本地服务和已有 Chrome 单标签展示。

## 当前检查点

- 已核验真实概览的重复快捷入口来自 `organization-shortcuts`；已有企业 dashboard 提供余额和完整成员预算，消费读取默认仅最近 100 条，不能作为周期统计。
- 已完成独立概览组件、纯计算模型和局部企业成品读取 hook；四指标/具体提醒/累计成员使用/最近六个真实成品与只读详情。未配置账户不当成零或正常，暂停预算/临期邀请排除失效成员。
- 已完成：`npm run typecheck` 初检；GG-044/GG-045/GG-048 定向测试 **24/24 通过**（GG-048 新增 8 项），无数据库/队列写入或真实 provider 请求。
- 首次完整 `npm run check:local`：404 项中 390 通过、14 opt-in 跳过、0 失败；lint/TypeScript/构建通过。32140 原进程已停止，使用已有真实接口栈安全配置启动最新 Web（会话 89695），没有重启容器/Worker；readiness 五项 ok、视频 available true。
- Computer Use 原 Chrome 1648143727 桌面检查：四指标/关注/成员行/团队资产空态正确，调整额度可打开原弹框，Escape 关闭，没有确认或 mutation。真实企业 `account: null`（后端 LEFT JOIN 账户不存在），有效成员 1、没有额度或成品；旧页默认 0 不是实际账户余额证据。
- 实测修正：把空账户提醒从临时不可用/刷新改成尚未配置/联系站长补充，避免误导恢复。修正后完整门禁 lint/TypeScript/构建与全部功能测试通过；404 项中 389 通过、14 opt-in 跳过，唯一失败为本卡缺少“下一步”文档字段，已补回；没有后续代码修改，使用文档定向复验，不重复构建。
- 文档字段补回后连续性 **8/8 通过**，`git diff --check` 通过；最终代码的 lint/TypeScript/构建/功能测试通过，唯一文档失败已定向修复验证，没有后续代码变更。
- 最终 Web 会话 **89176**，32140 readiness 五项 ok、视频 available true。原 Chrome 同企业详情 URL 已刷新截图确认“尚未配置企业积分”及联系站长提示；额度弹框/账户菜单收起，已标记保留供用户查看。Computer Use 只操作原 Chrome 标签，不使用 Playwright，不切换账户/上传/生成/划拨。
- 下一步：用户在 [企业概览](http://127.0.0.1:32140/organizations) 确认布局，确认后再按授权补完整周期聚合。本月统计后端未实现，有成品详情/窄屏实测待后续验收；不自动补积分、创建素材或任务。仅本地候选提交，用户 `.codex/` 配置保留且不纳入。
