# GG-027 — 企业与分销直属下级积分划拨

- 状态：阶段 0—1 已完成；阶段 2 待实现
- 用户需求：站长可指定企业或分销身份；符合身份的用户只能在自身余额范围内向直属下级分配积分。价格与收款完全场外处理，仅充值来源积分可转移，暂不做在线支付。
- 最后更新：2026-09-10
- 分支 / worktree：`feature/GG-027-distributor-credit-transfers` / `F:/goodgood-worktrees/GG-026`
- 基线：本地整合候选 `feature/GG-026-local-feature-integration@58feb26`；共同生产代码基线仍为 `origin/main@42fc8d81`，线上身份另查 CURRENT_STATE

## 范围与验收

- 要做：独立的企业/分销业务身份、单一有效直属上级、支付来源积分的可转移属性、直属下级零和划拨、用户与站长工作界面、简洁可追溯流水和完整审计。
- 不做：分销价格、人民币金额、在线支付、下游订单、佣金、返佣、提现、自主招募、隔级操作或上级强制追回。
- 验收点：企业和分销第一版共享同一划拨能力，但底层不以中文标签授权；只有站长可分配业务身份和调整关系；每个下级最多一个有效直属上级，拒绝自关联、循环与非直属操作。
- 验收点：只有支付订单形成的充值来源积分以及其后续转入余额可再次划拨；欢迎、测试、活动、运营赠送和普通调整均不可转移。现有积分不重置，无法证明为支付来源的历史积分一律不可转移。
- 验收点：划拨是不可编辑的原子零和转移；父级减少与子级增加同事务提交，具有同一公开编号、幂等键和审计。并发生成与划拨不能透支，失败不得产生单边余额或流水。
- 验收点：生成优先消耗不可转移积分；reserve 记录来源拆分，settle/release/refund 保持原来源。积分记录以充值/上级分配/分配给下级/生成等业务分类和公开编号追溯，不暴露内部账本 ID。
- 决策影响：[ADR 0043](../decisions/0043-direct-child-transfer-of-payment-funded-credits.md) 扩展 ADR 0006/0010/0020；已确认场外定价、仅充值来源可转、无在线支付。
- 授权边界：分阶段本地实现、迁移、自动测试和隔离 Compose 验收；不请求真实 provider，不推送、不合入 main、不构建或部署生产候选，线上发布由用户在本地验收后另行决定。

## 阶段划分

| 阶段 | 交付物 | 独立验收 |
| --- | --- | --- |
| 0. 决策与契约 | ADR、产品边界、数据/路由/异常/测试契约、任务恢复点 | 文档连续性测试与 diff 检查 |
| 1. 积分来源基础（已完成） | 向前迁移、充值/非充值来源投影、生成 reserve/settle/release/refund 来源守恒、余额读模型 | 账务单元测试、迁移重放/回滚、现有积分安全回填 |
| 2. 身份与直属关系 | 企业/分销业务身份、站长授权 API、单一直属关系、循环检查和审计 | 权限矩阵、关系约束、幂等/并发/API 错误测试 |
| 3. 零和划拨 | 直属下级列表、原子 transfer-out/transfer-in、公开编号、可分配余额与活动投影 | 余额守恒、并发不透支、重放、回滚、跨级拒绝测试 |
| 4. 页面与本地联调 | 站长身份/关系操作、企业/分销下级管理页、划拨确认、记录展示与响应式状态 | 组件/API/Compose/浏览器验收；最后仅运行一次完整本地门禁 |

阶段 1—4 依次建立在上一阶段的本地已验证提交上。任何阶段失败都停在本地修复，不跳过账务或授权基础直接制作界面。

## 实现与证据

- 相关文件/专题文档：`docs/PRODUCT.md`、`docs/PRODUCT_JOURNEY.md`、`docs/ARCHITECTURE.md`、`docs/DATA_MODEL.md`、`docs/ERROR_HANDLING.md`、`docs/UX_FLOWS.md`、`docs/ROUTES.md`、`docs/DESIGN_SYSTEM.md`、`docs/TESTING.md`、`docs/PROJECT_MAP.md`。
- 已完成：阶段 0 已将用户确认的三项边界和四段本地交付顺序写入 ADR 与专题契约；生产状态未改动。
- 已完成：阶段 1 新增迁移 `0020_gg027_credit_provenance.sql`，账户缓存支付来源可用/预留值，账本记录每次操作的支付来源部分；历史回填只认已支付订单证据，其他来源失败关闭为不可转。支付入账显式标记来源，生成优先扣除不可转积分，并在结算、释放和退款时保持原拆分；公开积分读模型增加 `transferableCredits`，现有侧栏仍显示总可用积分。
- 验证：阶段 0 文档连续性测试 8/8 与 diff 检查通过。阶段 1 最终快速账务/API/CI/文档定向测试 42 项中 38 通过、4 个数据库开关用例跳过、0 失败；隔离数据库实际执行全部迁移后，来源、账本、支付和手工支付共 19/19 通过。查询确认已支付 grant 全部等额标记、非支付正向积分无来源升级、账户来源约束 0 不一致；同时修正手工支付集成 fixture，使测试账户显式符合既有 active 入账前提。另从 3030 本地库复制只读快照到独立回填库，原库保持迁移 0019、2 个账户/4 条流水和 90/100 余额不变；副本单独应用 0020 后账户/流水计数及总余额完全一致，两账户可转余额均为 0，符合其欢迎/测试来源。两个测试库及临时 dump 已在验证后删除，可恢复的 3030 数据卷未改动。lint 与类型检查通过。完整本地门禁留到阶段 4 代码稳定后只运行一次。
- 发布：未发布；当前生产 revision、镜像和迁移保持不变。

## 恢复工作

- 尚未完成：阶段 2—4 的身份关系、零和划拨、页面、自动测试补全和 3030 本地验收。
- 阻塞/风险：本地分支叠加在尚未合入 main 的 GG-026 整合候选之上；必须保持这一依赖记录，不能从旧 main 重做并覆盖账户管理或积分记录。
- 下一步：实现阶段 2 的企业/分销业务身份、站长授权 API、单一直属关系、循环检查和审计。
