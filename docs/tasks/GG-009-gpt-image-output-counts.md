# GG-009 — GPT IMAGE 2 开放 2 / 4 张输出

- 状态：独立分支实现与完整本地门禁通过；待 PostgreSQL/组合联调和站长实测
- 用户需求：开放生成数量 `2` 和 `4`。
- 最后更新：2026-09-08
- 分支 / worktree：`feat/GG-009-gpt-image-output-counts` / `F:\goodgood`
- 基线：`main` @ `bab17fd4d4c4d88eec6194bb5d024769ca8e26fa`，依赖已独立实现的 GG-007 @ `e9b09d3fc5858aa42be9cc6dc9b7dbb120a705d3`。
- 决策：[ADR 0032](../decisions/0032-open-gpt-image-2-multi-output.md)

## 范围与验收

- GPT IMAGE 2 允许 `1 / 2 / 4`；一次点击仍是一个批次和一个上游任务，提交的 `n` 等于冻结的数量。
- 每张 10 积分，因此 1 / 2 / 4 张的批次价格为 10 / 20 / 40；预留、成功结算和失败释放仍按整批原子处理。
- Worker 必须校验上游返回数量，下载和解码每张图片，按 ordinal 存入私有对象存储，并在一个事务中写入全部 Asset。
- 任务与资产 API 返回同批全部图片且顺序稳定；创作流、项目和资产库均能显示。
- 首版整批成功或整批失败，不公开部分结果。失败时清理本次已写对象并保留现有恢复入口。
- Nano Banana 2 当前上游契约没有数量参数，继续只支持 1 张；切换模型时将不兼容数量归一为 1。
- `npm run check:local` 通过；组合到 3010 后只做无费用 UI/接口核对，由站长决定是否触发真实生图。
- 不包含生产发布、真实付费上游冒烟、Nano 多输出或部分结果结算。

## 当前检查点

- 已完成模型级 count 能力、模型切换归一、不可用按钮、GPT `n`、严格返回数量校验、全批输出下载/解码/失败清理、ordinal Asset 数组、10/20/40 报价和迁移 0014。
- 已覆盖 GPT 的 7 比例 × 3 分辨率 × 3 数量共 63 个提交组合、四图顺序、短结果拒绝、存储中途失败清理、多 Asset API 输出和价格结构。
- 验证：`npm run check:local` 通过，共 200 项，196 通过、4 个 opt-in 集成测试跳过、0 失败；`git diff --check` 通过。未调用真实付费上游。
- 尚未验证：迁移 0014 的真实 PostgreSQL 四 Asset/40 积分事务；合并 GG-004—GG-008 后的组合门禁、3010 UI 和真实生成由站长实测。
- 下一步：提交独立分支，合入本地组合候选，重建 3010 后运行 PostgreSQL 专项与无费用浏览器核对。
