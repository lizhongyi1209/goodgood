# GG-009 — GPT IMAGE 2 开放 2 / 4 张输出

- 状态：3010 真实 O1Key 联调环境已就绪；待站长真实生成验收
- 用户需求：开放生成数量 `2` 和 `4`
- 最后更新：2026-09-08
- 当前组合分支：`test/GG-004-GG-005-local`
- 独立实现：`feat/GG-009-gpt-image-output-counts` @ `0d827ed`
- 决策：[ADR 0032](../decisions/0032-open-gpt-image-2-multi-output.md)

## 范围与验收

- GPT IMAGE 2 允许 `1 / 2 / 4`；一次点击仍是一个批次和一个上游任务，提交的 `n` 等于冻结的数量。
- 每张 10 积分，因此 1 / 2 / 4 张的批次价格为 10 / 20 / 40；预留、成功结算和失败释放仍按整批原子处理。
- Worker 校验上游返回数量，下载并解码每张图片，按 ordinal 存入私有对象存储，并在一个事务中写入全部 Asset。
- 任务与资产 API 返回同批全部图片且顺序稳定；创作流、项目和资产库均能显示。
- 首版整批成功或整批失败，不公开部分结果。失败时清理本次已写对象并保留现有恢复入口。
- 本任务交付时 Nano Banana 2 仍只支持 1 张；后续 [GG-010](GG-010-banana-multi-output.md)
  已接受以多上游任务编排方式开放 `1 / 2 / 4`，取代这里的当前能力描述。
- 不包含生产发布、真实付费上游冒烟、Nano 多输出或部分结果结算。

## 当前检查点

- 已完成模型级数量能力、模型切换归一、不可用按钮、GPT `n`、严格返回数量校验、全批输出下载/解码/失败清理、ordinal Asset 数组、10/20/40 报价和迁移 0014/0015。
- 已覆盖 GPT 的 7 比例 × 3 分辨率 × 3 数量共 63 个提交组合、四图顺序、短结果拒绝、存储中途失败清理、多 Asset API 输出和价格结构。
- 组合门禁：`npm run check:local` 通过，共 212 项，206 通过、6 项 opt-in 集成测试跳过、0 失败；`git diff --check` 通过。
- PostgreSQL 专项 5/5 通过：迁移校验、四 Asset/40 积分事务、短结果回滚和完整报价均已验证；测试保留既有本地账号余额。
- 最终镜像已重建：迁移容器退出码 0，Web/Worker/PostgreSQL/Valkey/RustFS/提供方探针健康，`/api/health/ready` 全部为 `ok`，最新迁移为 0015。
- 无费用浏览器核对通过：GPT 2/4 均可选，4 张显示 `10 积分/张 · 共 40`；切换 Nano 自动归一为 1 并禁用 2/4，切回 GPT 后恢复可选。页面保留在 GPT、2 张、共 20 积分。
- 未由 agent 调用真实付费生成；站长下一步在 3010 实测 2/4 张批次及连续并行点击。
