# GG-014 — 精简侧栏积分余额

- 状态：本地已验证；3010 Web 已就绪，未发布
- 用户需求：删除左下角积分余额区域的 `10 积分/张 · 可生成 0 张`
- 最后更新：2026-09-08
- 分支：`feature/GG-014-sidebar-balance-copy`
- 依赖基线：本地组合候选 `75deca2`，包含 GG-004–GG-013；本任务保持当前 3010 候选能力不回退
- 决策：[ADR 0035](../decisions/0035-simplify-sidebar-credit-summary.md)

## 范围与验收

- 桌面端左下角正常积分状态只显示 `积分余额` 和当前余额数字。
- 删除固定单张价格及“可生成张数”的第二行，不保留空白占位。
- 创作器操作区的当前模型单张价格和多图批次总价继续显示。
- 积分加载、读取失败/重试、零余额和移动端余额显示行为不变。
- 更新设计决策和自动化契约测试；不调用真实 provider。
- 不包含生产部署、计费规则修改或数据库变更。

## 实现与证据

- 已从页面移除侧栏启动报价、可生成张数派生和第二行文案；同时清理对应未使用样式。
- 创作器继续使用当前模型与输出数量的活动报价。
- 定向计费/UI 回归 18/18 通过；新增 ADR 首次遗漏索引后已补齐并以文档连续性测试
  8/8 验证。最终 `npm run check:local` 共 228 项（222 通过、6 项 opt-in 跳过、
  0 失败），构建、Lint 和类型检查通过。
- 3010 Web 已用当前源码重建，readiness 的 database/objectStorage/provider/queue/runtime
  全部为 `ok`；真实 Chrome 项目页核对侧栏仅显示 `积分余额 0`，创作器仍显示
  `10 积分/张 · 共 20`，未触发真实生成。
- 发布：未发布，生产环境未变。

## 恢复工作

- 尚未完成：站长视觉验收与生产发布决定。
- 阻塞/风险：无外部阻塞；本任务从已验证的本地组合候选继续，未从生产版本单独切出。
- 下一步：站长在当前 3010 页面核对左下角精简结果。
