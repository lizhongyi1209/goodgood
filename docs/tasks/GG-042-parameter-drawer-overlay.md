# GG-042 — 参数抽屉覆盖成品区

- 状态：本地完成并验证，待用户检查
- 更新：2026-09-13
- 分支/目录：`fix/GG-042-parameter-drawer-overlay` / `F:/goodgood-worktrees/GG-037`
- 基线：已验证的 GG-041 `5ae3521`，干净依赖候选上顺序新分支；main 与其他 worktree 不变。
- 决策：[ADR 0055](../decisions/0055-parameter-drawer-overlay.md)

## 验收与检查点

- 图片/视频参数抽屉向下贴边覆盖，不占位、不推移成品区；开启层级提高一级。
- 不改变参数、数量/并发、报价、参考素材或生成行为。
- 窄屏及长提示词时，抽屉内部可滚动；收起控件不可点击或键盘聚焦。
- 已实现：共享绝对定位抽屉、开启 composer 层级 `24 → 25`、闭合 inert/不可点击、ResizeObserver 与窗口滚动/尺寸变化驱动剩余视口高度。保留原参数布局、动画和设置开关。
- 定向测试 29/29；完整 `npm run check:local` 通过：370 项中 356 通过、14 个 opt-in 跳过、0 失败；lint、TypeScript 与构建通过。新增测试的 lint 命名冲突已修正后重跑通过。
- 现有 Chrome computer use 已核对图片/视频桌面展开与收起，成品相同位置、抽屉贴边覆盖；临时 `390×844` 视口核对双模式，视频八行提示词下内部滚动仍可达底部数量 4。视口已恢复，测试页重新加载并保留供用户检查。
- 下一步：用户确认 32139 本地样式；不自行扩展到生成或发布。
- 仅本地 UI，不提交生成，不触碰 32138 真实结果页，不推送/合入/部署。
