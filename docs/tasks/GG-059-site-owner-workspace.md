# GG-059 — 大厅统一站长管理

## 状态与基线

2026-09-13：本地实现、完整门禁与原页面浏览器检查完成，未发布。分支 `feature/GG-059-site-owner-workspace`，
worktree `F:/goodgood-worktrees/GG-059`，接续已验证 GG-058 `5eb1695`；
main 已核验为祖先，原工作树与用户本地配置保留。

## 范围

- 站长侧栏/移动入口统一为站长管理，默认企业管理，右侧切企业/模型/账户。
- 既有管理 URL 共用大厅 shell，导航不整页跳转，创作内存/任务保留。
- 管理数据逻辑与授权保留，嵌入视图共用当前登录；普通企业入口不变。
- 模型/账户成功修改后刷新大厅价格、可用性、余额与企业目录；嵌入页面
  不重复挂载品牌页头或 Toaster。窄屏管理状态提供返回创作而非直接退出。
- 按 ADR 0067 调整 ADR 0066 的独立页面决定；字体修复与无直接退出保留。
- 仅更新 32141 local/mock Web，不改价、不迁移、不重设数据、不付费生成；
  不碰真实 provider 32140、不推送/合 main/部署。

## 验证

- 新 GG-059 路由/历史/视图/授权/共享数据刷新定向 5/5 通过，原导航与
  UI 组件定向 32/32 通过。SSR fixture 仅内存，不连接数据库/provider。
- 首次完整门禁 437 通过/16 opt-in 跳过/0 失败，lint/类型/构建通过。
- 窄屏检查暴露原大厅头像直接退出且缺少离开管理区入口；限定站长管理状态
  将头像位置改为返回创作，保留此前无直接退出决定。因该运行代码修改，重跑
  一次完整门禁（40180）：453 项中 437 通过/16 opt-in 跳过/0 失败，lint/
  类型/构建全部通过；最终日志 ignored `.gg052-gg059-check-final.log`。
- 停止初版 Web 16840，确认 32141 无监听后从 GG-059 最终构建启动 Web
  82667；Worker 36299/mock 62817 继续来自未变的 GG-057，三角色 ready
  且全部 checks 为 ok。独立 `goodgood-gg052` 配置不变，无迁移或 fixture。
- Chrome 原站长标签 1648144149：唯一侧栏站长入口，默认企业，右侧三功能
  往返与侧栏保留；创作往返 performance.timeOrigin 完全相同，原 108 积分/张
  选项保留。Back/Forward 和原 `/admin/users` 地址刷新仍显示大厅，登录有效。
- 390×844 窄屏切换与价格弹窗可用，Banana 2 优质读取为 0.40 元，取消且
  没有保存。最终构建窄屏管理无退出按钮、返回创作正常；恢复正常窗口，最后
  `/admin/models` 页面标记保留。没有生成或价格/账户/积分写入。
- 模型全记录与九类历史 count/hash 前后精确一致：管理事件 12/价格版本
  120/积分流水 4/积分账户 2/批次、任务、尝试、资产、项目各 1。copied helper
  沿用 `.gg052-gg057-before.json` / `after.json` 文件名，位于 GG-059。
  草稿正常读取/保存属于大厅既有行为，没有造 fixture 或删除原创作内容。
- 最终文档连续性/发布边界 15/15 通过；本地 helper、日志、数据库、秘密
  与截图均不进入 diff。当前中文字体设置保留，不新增 Windows 栅格化保证。

## 下一步

用户在 `http://127.0.0.1:32141/admin/models` 尝试定价；点击侧栏站长管理
默认进入企业，再用右侧三功能栏切换，点击创作（窄屏返回创作）离开。
本地实现与验证完成；正式生产、32140、价格/历史保留，无推送/合 main/
部署或付费请求。发布仍需独立明确授权。
