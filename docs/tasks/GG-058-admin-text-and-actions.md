# GG-058 — 站长页面文字与右上角操作

## 状态与基线

2026-09-13：本地调整、完整门禁与页面/浏览器检查完成，原 Windows 重影待用户复看，尚未发布。分支 `fix/GG-058-admin-text-and-actions`、worktree `F:/goodgood-worktrees/GG-058`，接续 GG-057 `ddb465b`；main 已核对为祖先，保留原候选与无关工作树。

## 范围

- 用户报告模型管理切回账户管理后字体重影；两页右上角只保留返回创作，不直接展示退出。
- 检查原 32141 切换前后：标题仅一份、无 text-shadow、body 字体与 stylesheet 相同；未发现重复 DOM 或重复页面样式。当前字体栈缺少明确中文字体，浏览器诊断当前环境实际使用系统 Noto Sans SC；这不能证明用户 Windows 截图的实际字体。
- 针对站长页面补充明确中文无衬线字体与关闭合成字重，避免依赖平台默认中文回退。修改限定两页主体，不改变全局创作文字或增加重绘/缩放技巧。恢复后浏览器重复切换与截图检查，保留不能跨平台证明的限制。
- 移除共享页头的退出按钮和不再需要的回调；返回创作/账户/模型导航保留。退出仍在创作账户菜单以及 pending/suspended 访问门禁中，后端注销契约不变。
- 用户直接细化 ADR 0066 的页头操作决定，先更新该 ADR。模型价格/启禁/积分/历史与登录逻辑不变，无迁移或数据写入。
- 更新原 32141 独立 local/mock 候选供用户检查；不触碰 32140、不调用付费 provider、不合 main/推送/部署。

## 验证

- 导航/登录/文档/发布边界定向 25/25 通过。已有导航测试改为确认页头不展示退出，访问门禁的注销回调保留；没有新增镜像 CSS 实现的测试。
- 一次 `npm run check:local` 完成：lint/TypeScript/构建通过，448 项中 432 通过/16 opt-in 跳过/0 失败。构建前调整字体栈顺序，让中文明确字体先于通用字体；最终编译 CSS `index.C1a6m4Vg.css` 含最终顺序和 `font-synthesis:none`。门禁完成后没有运行代码修改，日志 ignored `.gg052-gg058-check.log`。
- 原 GG-057 Web 会话 12422 停止，确认 32141 无监听后从 GG-058 启动 Web 会话 87779。Worker 36299/mock 62817 继续来自 GG-057，逻辑未变无需重启；三角色 readiness 全通过。local/mock 配置与 `goodgood-gg052` 数据栈不变，没有迁移或 SQL fixture 写入。
- Chrome 原标签 1648144149 更新账户页；模型/账户往返两轮后标题仍只有一个、字体栈与 `font-synthesis:none` 正确，截图未见重复文字或阴影。两页右上角仅返回创作，没有退出按钮。返回创作正常且账户菜单/站长入口保留；最后账户页正常窗口标记保留。
- 新旧全部模型记录与九类历史 count/hash 精确一致。读取快照沿用 copied GG-057 helper 的 ignored `.gg052-gg057-before.json` / `after.json` 文件名，文件位于 GG-058；管理事件 12、价格版本 120、积分流水 4、积分账户 2，批次/任务/尝试/资产/项目各 1。不写价格、启禁、账户或积分。
- 当前浏览器字体证据无法确认用户原 Windows 截图的栅格化原因；交付明确字体与禁止合成字重的局部改善，不宣称复现或彻底消除 Windows 特定重影。
- 最终文档连续性/发布边界 15/15、diff 检查通过；没有 secret、截图、数据库或本地运行 helper 进入 diff。

## 下一步

用户在 `http://127.0.0.1:32141/admin/users` 查看原 Windows 切回后的文字效果；如仍存在，下一步优先读取该原浏览器实际字体/缩放及原尺寸截图再定位，不凭截图猜测重复 DOM。当前页面调整与本地门禁已完成，运行与价格/历史检查通过。正式生产和 32140 不变，没有推送/合 main/部署或真实付费请求。
