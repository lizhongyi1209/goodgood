# GoodGood 当前状态

- 最后核对：2026-09-08；这是截至该日的已记录结果，不是实时监控。
- 产品阶段：已开放的 `controlled-alpha-v1`，不是完整 seed/付费生产就绪。
- 正式入口：https://goodgood.o1key.com
- 当前工作：GG-004 至 GG-010、GG-012 至 GG-019 本地组合候选已在 3010 真实 O1Key 联调
  环境就绪，等待站长实测；GG-011 已固化快速交付与测试隔离规则，不需部署。
  产品候选均未部署。
- 下一个普通产品需求从 GG-020 分配。不要自动恢复搁置的 C6。

## 已上线的能力与边界

- Authing Google/邮箱验证码登录；后端验证身份，使用 GoodGood 自有会话。
- 注册人数不设上限；新账户 `pending`，发放 100 欢迎积分，审核后才能创作。
- 三种账户状态：`pending / active / suspended`。站长在 `/admin/users`
  审核、暂停账户及审计式补发测试积分；现有单次补发范围为 1–5000。
- Nano Banana 2：14 种产品宽高比 × `1K / 2K / 4K`，共 42 个组合。
  一次输出 1 张、10 积分。其他模型的 UI 名称不代表已接入；多输出未开放。
- 线上仍以 `标准 / 高清 / 超清` 展示分辨率，资产库尚未展示实际像素尺寸；
  GG-005 本地候选将修改该展示，生产事实以发布后的复验为准。
- GPT IMAGE 2 的 `gpt-image-2-c-sd`、精确尺寸和 1/2/4 张输出均仍是本地候选，
  不能当作已上线能力。
- 真实后端任务、积分预留/结算/失败处理、最多 10 张参考图、私有结果读取、
  资产库、项目保存恢复、创作草稿均已有实现；具体语义以对应代码/测试为准。
- 已确认 GG-004 可靠性缺陷：同一 queued job 可被 Web/Worker 重复派发并在单 Worker
  内并发执行，导致实际生成并上传私有 R2 的结果仍被记录为 `SUBMISSION_UNKNOWN`，
  数据库无 Asset 且积分被释放。修复已在本地阻止重复执行并校验终态提交；生产仍运行
  `94cecb0`，线上尚未修复，真实数据未做人工恢复。
- 保持香港现有服务器、现有私有 R2；支付/支付宝、自动账户删除、举报界面、
  Grafana/复杂监控及大规模上线配套暂缓。
- 不新增用户任务数或生成并发上限。持久队列用于可靠投递/恢复，不是人为串行限制。
  继续执行可用内存低于 500 MiB、根磁盘使用率达到 80% 的新生成保护。
- 站长只激活已亲自确认并告知测试边界的用户：非敏感素材、O1Key 处理、
  外部删除条款尚未齐备、站长提供线下反馈/暂停/精确目标人工处理渠道。
  这是 ADR 0024 的补偿措施，不得宣传完整隐私删除或付费服务保障已完成。

## 生产身份与环境

| 项目 | 最近核验记录 |
| --- | --- |
| 源码 revision | `94cecb0aa53f7adf1dd6c3b6be23325a535ed5c0` |
| 镜像 | `ghcr.io/lizhongyi1209/goodgood@sha256:fe52e00933367a7f8575dc862f41550da514a4f1324526224bf347d1934127b3` |
| 数据库迁移 | `0012_m8_remove_legacy_local_fixtures.sql` |
| 配置契约 checksum | `05fc1ed4bf1848f6a2611fd63a29e1c0de168aaf914743507f70e7434aac158a` |
| 活跃进程 | green Web + 1 个 green Worker |
| 回退候选 | blue `30c7a73` 已停止保留；实际回退仍须核验兼容性 |
| 主机 | 香港 2 vCPU / 4 GiB / 50 GiB；Web、Worker、PostgreSQL、Valkey 同机 |
| 对象与备份 | 私有 R2；加密异机数据库备份，RPO 1h / RTO 4h，14 日/8 周/12 月 |
| 本地 | Windows 开发；Compose 使用本地 PostgreSQL/Valkey/RustFS/mock |

没有常驻远程测试环境。`staging-goodgood.o1key.com` 仅保留名称，非当前测试入口。
SSH 别名 `goodgood-staging` 是历史命名，指向现有生产主机，不能据名字当作测试机。
生产目录 `/opt/goodgood-production`，受保护配置 `/etc/goodgood/production`；
不要将任何生产数据或凭据拷贝回本地。主机地址和密钥不在本文存放。

## 最近验证（区分证据层次）

- GG-006 候选：完整本地门禁 193 项（189 通过、4 个 opt-in 跳过、0 失败）；真实本地栈
  全部 ready。浏览器核对桌面 `64 × 64`、移动端 `56 × 56` 的中心裁切和响应式参数顺序。
  该证据不是生产发布验证。
- GG-004 候选：`npm run check:local` 共 201 项，195 通过、6 个 opt-in 跳过、0 失败；
  PostgreSQL 并发派发、积分/lease 集成及本地 Compose 重复投递回归均通过。未做发布验证。
- GG-004—GG-010 组合候选完整门禁 216 项（210 通过、6 个 opt-in 跳过、0 失败）：
  活动任务期间发送按钮保持可用，25 个并行客户端运行状态未截断，GPT IMAGE 2
  GPT IMAGE 2 与 Nano Banana 2 均支持 1/2/4 张与 10/20/40 积分，同时保留
  精确尺寸和投递竞态保护。隔离 PostgreSQL 专项 5/5 通过，验证 Nano 四
  Asset/40 积分原子结算和短结果回滚。
- GG-012 候选：最终 `npm run check:local` 共 222 项（216 通过、6 个 opt-in 跳过、
  0 失败）；定向测试 64/64。测试覆盖默认字段省略、高/开启顶层转发、模型隔离、
  快照/持久化以及 `TEXT` + `IMAGE` 响应模态，全部使用 stub，未调用付费上游。
  站长反馈关闭态开关不可辨认后，谷歌搜索已改为清晰的“关闭 / 开启”双选按钮，
  3010 浏览器已复核两个状态的可见选中反馈。
- GG-013 候选：最终 `npm run check:local` 共 228 项（222 通过、6 个 opt-in 跳过、
  0 失败）；下载与相关 UI/API 定向测试 25/25。真实 Chrome 复现确认 0 字节故障源于页面缓存仍显示图片时 15 分钟预览签名已过期，下载重新读取该旧地址失败。
  当前下载按 Asset ID 即时获取新的所有者鉴权签名，再读取校验并交给浏览器下载管理器；3010 Web 已重建且健康，新签名实测 HTTP 200、`image/jpeg`、534742 字节，跨所有者读取返回 404，真实点击命中新接口且浏览器无阶段错误。最终系统保存窗口仍待站长确认。所有结果保持原始色彩一致显示。当次诊断时账户积分为 0，
  未执行真实付费生成；用户执行过文件保存验收，0 字节测试文件未由代理删除。
- GG-014 候选：桌面侧栏积分成功态已精简为 `积分余额` 与当前数值，不再重复固定单张
  价格或估算可生成张数；创作器的当前模型单张/批次报价保持不变。3010 Web 已重建并通过
  readiness，真实 Chrome 页面核对侧栏仅显示 `积分余额 0`，创作器仍显示当前批次报价；
  最终 `npm run check:local` 共 228 项（222 通过、6 项 opt-in 跳过、0 失败）。未调用
  真实 provider，未发布。
- GG-015 候选：GPT IMAGE 2 已新增质量 `自动/低/中/高`、背景 `自动/透明` 和输出格式
  `PNG/JPEG/WebP`；输出格式默认值已由 PNG 调整为 JPEG，并显式进入快照、草稿、项目、
  批次和 O1Key v2 顶层字段。
  透明背景会把既有 JPEG 自动切为 PNG，并在透明状态禁用 JPEG；服务端在预留积分和
  provider POST 前重复拒绝非法组合。最终 `npm run check:local` 共 234 项（228 通过、
  6 项 opt-in 跳过、0 失败）。3010 迁移 0018、readiness、无 v1 活动 attempt 和真实
  Chrome 跨模型切换后的 JPEG 默认选中均已核对；代理未点击生成。站长已完成一次真实
  `3:4 / 1K / 2 张 / 自动 / 自动 / JPEG` 生成，任务成功并落库 2 个 Asset，积分
  `500 → 480`、冻结积分为 0；透明输出与最终下载仍待继续验收。未发布。
- GG-016 候选：Nano Banana 2 已移除创作器和图片详情中的“思考程度”，新请求内部固定
  为 `high` 并向 O1Key 每个任务发送顶层 `thinking_level: "high"`；谷歌搜索交互不变，
  历史显式低思考任务仍可按旧载荷重试。定向测试 55/55；最终本地门禁 235 项（229
  通过、6 项 opt-in 跳过、0 失败）。3010 Web/Worker readiness 和真实 Chrome 参数抽屉/
  图片详情核对通过，代理未点击生成。未发布。
- GG-017 候选：已上传并校验通过的参考图作为所有者隔离的持久素材保留，资产库新增
  “上传素材”板块，创作器可在“上传本地图片 / 从资产库选择”之间切换并多选复用稳定
  素材 ID；同一托盘去重且继续遵守 10 张上限。ready/accepted 素材不再进入 30 天孤立
  清理，历史尚未删除的 `REFERENCE_ORPHANED` accepted 素材会被救回。最终
  `npm run check:local` 共 239 项（233 通过、6 个 opt-in 跳过、0 失败）。3010 readiness
  全部为 `ok`；当前账号列出 3 个素材，另一所有者为空；真实 Chrome 已验证来源菜单、
  方形素材选择器、加入托盘及资产素材视图。验证前后活动 generation job/attempt 均为 0，
  代理未点击生成。未发布。
- GG-018 候选：参考图托盘左下角持续显示连续的 `图 1…图 10`，支持拖到另一项位置
  以及聚焦后使用 `Alt + ← / →` 调序；显示、草稿/显式项目保存及生成请求沿用同一
  参考数组顺序。完整 `npm run check:local` 共 239 项（233 通过、6 个 opt-in 跳过、
  0 失败）。3010 真实 Chrome 已验证三图编号、首图拖至末位、键盘左移和即时重编号，
  控制台无错误；代理未点击生成。未发布。
- GG-019 候选：ready 参考图可点击或用 Enter/Space 打开聚焦大图，Escape 或显式按钮
  关闭；预览按完整原图约束在视口内，不裁切。右上角删除控件缩为 16 × 16 px、距上/右
  各 3 px，删除不触发预览；拖拽结束也不会误开。最终 `npm run check:local` 共 239 项
  （233 通过、6 个 opt-in 跳过、0 失败）。3010 真实 Chrome 已验证 900 × 1199 原图在
  1146 × 772 图片边界内完整显示、点击/键盘/关闭/拖拽行为和无控制台错误；最终活动
  generation job/attempt 均为 0，代理未点击生成。未发布。
- 3010 联调栈迁移退出码 0，Web/Worker/PostgreSQL/Valkey/RustFS/提供方探针均健康；
  Web readiness 全部检查为 `ok`，最新迁移为 0018。浏览器确认 GPT 与 Nano
  2/4 可选及 20/40 批次报价；Nano 仅显示谷歌搜索，内部高思考不在参数抽屉或详情
  暴露。隐藏高思考的真实付费生成仍待站长按需实测。
- 发布源码：本地门禁 187 项（183 通过、4 个 opt-in 跳过）；CI run 37
  `34127634812` 的源码/依赖/运行时/镜像安全检查通过。
- 上线后：公网首页/readiness、登录跳转、未登录拒绝生成、HTTPS 转向、
  green 健康、单 Worker 交接、备份及兼容切换检查通过。
- 获批的单次真实 `4:5 / 2K` 生图通过，私有 R2 字节/哈希一致，积分 100→90，
  队列归零。两次 API 资产列表读取证明持久化；临时测试会话已撤销。
- 本次扩展后的浏览器自动化受工具超时影响，未完成新的 UI 全流程复验。
  API 验证不能被写成“浏览器重登录已验证”；早期 C6 用户手动重登录是另一份证据。
- 完整非敏感摘要：[发布记录](releases/2026-09-07-banana-2-parameters.md)。
  本次整理的验证见 [GG-001](tasks/GG-001-project-continuity.md)，不能沿用上述测试数。

## 仓库与搁置工作

- `main`：经核验的集成基线；文档提交或待部署功能可领先线上。
  **分支名不等于生产版本**，新会话必须检查 Git 和以上部署身份。
- 每个新需求建自己的分支/任务卡；并行窗口必须使用独立 worktree。
- 历史 C6：`archive/c6-deletion-content-safety-20260907`，初始完整快照
  `d65838a077aeb21e0f7327d6b82ce696d0363a47`，已推送到 origin。
  本机目录 `F:/goodgood-worktrees/c6-deferred`；独立 Git bundle 备份
  `F:/goodgood/work/c6-preserved-20260907.bundle` 已校验；原 stash 保留。
- 快照包含原 118 个改动文件、迁移 0013–0020、删除/举报及 alpha 运维工具。
  **都不是目前部署内容，不得整体混入下一次功能发布。** 见 GG-900。
- 干净基线尚无 `production:alpha-gate` 命令；以前使用的工具在上述快照。
  下次发布前按 GG-003 单独提取/验证，不可假称全 seed 门禁已通过。
- 完整旧计划在 `docs/history/`，按需追溯；不要求新窗口全量阅读。

## 更新规则

仅在实际状态改变或重新核验后更新，替换旧摘要而非不断追加。
代码完成、本地通过、CI 通过、已部署分别记录；未知写“未验证”。
发布需同时更新本页和发布记录；具体任务进度在任务卡，优先级在 BACKLOG。
