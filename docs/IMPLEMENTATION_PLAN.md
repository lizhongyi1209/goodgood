# Production implementation plan

- Last synchronized: 2026-09-09
- Current phase: 已开放 controlled alpha；转入按需求的高频小步迭代。
- Current objective: 站长已要求把当前累计候选发布到生产。发布前置 GG-003 正在
  `feature/GG-003-alpha-release-tooling` 从历史快照最小提取独立 alpha 门禁；当前
  schema v2、候选绑定、时效和 CLI 失败路径已通过 18/18 定向测试；完整本地门禁
  251 项中 245 通过、6 项 opt-in 跳过、0 失败。当前等待 CI 精确摘要和主机新鲜
  证据。候选包含 GG-004～GG-021；Nano Banana Pro
  只增加 `1K / 2K / 4K` 单张 15 积分价格，不开放 Pro provider 路由。

## Current checkpoint

- 生产入口 `https://goodgood.o1key.com`；最近已部署源 `94cecb0`，迁移 0012。
  完整身份、能力边界及最近证据只维护在 [CURRENT_STATE.md](CURRENT_STATE.md)。
- C6-3 / C7 的小范围用户开站已完成。ADR 0025 全宽高比/分辨率已上线。
  这不是完整 seed/paid 就绪，仍需站长审核账户，支付不开放。
- GG-001：旧 C6 的 118 个改动文件已提交/推送到独立 archive 分支，另有 worktree
  与校验通过的 bundle；原 stash 保留。旧计划归档，入口/任务协议已整理。
  本轮完整本地门禁通过，交接结果见 [任务卡](tasks/GG-001-project-continuity.md)。
- GG-004 与 GG-005 已在各自独立分支完成本地验证，均未上线；GG-006 从 `main`
  独立实施，最终只在本地联调分支组合三项，避免把并行任务混入单项交付分支。
- 生产与本地分开：文档整理不部署、不迁移、不修改真实账户/素材/积分。
  历史记录中的开站、轮换、清空等许可不继承，旧转换流程不是新的执行计划。
- GG-004：一条线上任务被重复派发并由同一 Worker 并发执行；失败分支先写入
  `SUBMISSION_UNKNOWN`，成功分支已取得结果并写入私有 R2，却未能提交 Asset。
  本地候选已加入 outbox 原子领取与延迟重派、单 job 互斥、严格 lease、终态结果检查和
  孤儿对象补偿；完整门禁及 PostgreSQL/Compose 专项回归通过。积分已释放，生产数据
  未做修复；完整非敏感证据见[任务卡](tasks/GG-004-generation-dispatch-race.md)。
- GG-005：ADR 0027 已确认直接展示分辨率域值，并以 accepted Asset 持久化的实际宽高
  作为资产尺寸来源；不修改数据库和 provider 契约。
- GG-006：ADR 0029 取代被初审否定的矩形方案，最终使用桌面 64 px、移动端 56 px 的
  1:1 居中预览及“比例—模型—输出”顺序；完整门禁、真实本地栈和浏览器响应式核验通过。
- GG-007：已完成 GPT IMAGE 2 的七种比例、精确 1K/2K/4K 像素、
  `gpt-image-2-c-sd` O1Key 路由、参考图和三档 10 积分；独立与组合门禁、迁移和浏览器核验通过，尚未调用付费上游。
- GG-008 已在独立分支实现：每次点击创建独立运行，活动任务期间发送仍可用，
  产品侧不设并发任务数上限；一个点击冻结当前所选数量并独立预留对应积分。本地门禁
  193 项（189 通过、4 个 opt-in 跳过）；组合门禁 207 项（201 通过、6 个 opt-in 跳过）。
- GG-009：GPT IMAGE 2 已开放 1/2/4 张，单批对应 10/20/40 积分；该任务交付时
  Nano 仍只支持 1 张，后由 GG-010 取代。组合门禁 212 项（206 通过、6 个
  opt-in 跳过），PostgreSQL 专项 5/5 通过，
  3010 浏览器已核对数量、报价和模型切换归一，未触发成功付费生成。
- GG-010：Nano Banana 2 已开放 1/2/4 张，2/4 张使用有序 O1Key 单图任务集；
  每次提交前后的持久证据阻止不确定 POST 自动重放。完整门禁 216 项（210 通过、
  6 跳过），隔离 PostgreSQL 专项 5/5，迁移 0016、Compose/readiness 和浏览器
  1/2/4 与 10/20/40 报价均已验证。
- GG-011：根入口、工作流、测试策略和历史经验已加入最小改动、定向优先、完整门禁
  一次及真实 provider 隔离规则；文档契约测试 8/8 通过，未重建 Compose。
- GG-012：Nano Banana 2 请求固定使用 `TEXT` + `IMAGE` 响应模态；谷歌搜索进入不可变
  快照、迁移 0017 和 O1Key v4 顶层字段。原低/高思考 UI 已由 GG-016 取代。
- GG-013：活动骨架和完成图改用同一创作网格，稳定槽位在提交时形成并原位替换；
  创作卡片显示具体像素，创作卡片与图片详情移除重复收藏和所有按序号调色；创作卡片下载按钮补齐交互反馈，本地签名对象 CORS 补齐 `GET/HEAD`。真实 Chrome 诊断确认缓存可见但已过期的 15 分钟预览签名导致下载失败；ADR 0034 已修订为按 Asset ID 即时获取所有者鉴权新签名，再读取校验并使用延迟释放对象 URL 的浏览器下载管理器保存。完整门禁
  228 项（222 通过、6 项 opt-in 跳过），3010 Web 已重建并通过 readiness、所有者隔离、新签名字节读取和真实按钮点击，待站长完成系统保存窗口复核。
- GG-014：ADR 0035 取代桌面两行积分摘要；侧栏只保留 `积分余额` 与数值，并移除该处
  固定启动报价和估算张数的派生。创作器报价、移动端余额、加载和重试语义不变；3010
  Web readiness 与真实 Chrome 文案核对通过；完整门禁 228 项（222 通过、6 项
  opt-in 跳过），未调用真实 provider。
- GG-015：ADR 0036 取代 GPT 输出参数延期决定；质量/背景/输出格式已贯穿快照、草稿、
  项目、批次、重试和详情，O1Key GPT 路由升级为 v2，输出格式默认值后续修订为 JPEG。
  迁移 0018、最终 234 项门禁、readiness 和真实 Chrome 跨模型 JPEG 默认选择通过；
  站长真实 `3:4 / 1K / 2 张 / 自动背景 / JPEG` 生成成功并落库 2 个 Asset，余额 480、
  冻结 0，代理未点击生成。
- GG-016：ADR 0033 已修订；Nano 创作器与图片详情不再显示思考程度，新创作状态和
  上游任务默认固定为 `high`，谷歌搜索不变，历史显式低思考重试保持原载荷。定向
  55/55、完整门禁 235 项、3010 readiness 与真实 Chrome UI/详情核对均通过；未触发生成。
- GG-017：ADR 0037 已接受；上传并校验通过的参考图成为所有者隔离的持久素材，资产库
  提供独立“上传素材”板块，创作器可从本地上传或从素材库多选复用稳定 ID。ready/accepted
  素材不再按 30 天孤立对象清理，历史未删除的误标素材可被救回。完整门禁 239 项（233
  通过、6 个 opt-in 跳过），3010 readiness、跨所有者列表隔离、浏览器来源菜单/选择器/
  托盘复用与资产视图均通过；活动 generation job/attempt 为 0，未触发真实生成。
- GG-018：ADR 0038 取代托盘不显示编号的旧决定；参考图左下角持续显示连续序号，支持
  原生拖拽和 `Alt + ← / →` 排序，页面状态直接重排既有参考数组。完整门禁 239 项（233
  通过、6 个 opt-in 跳过）；3010 Chrome 已验证三图序号、首图拖至末位、键盘左移与
  无控制台错误，未点击生成。
- GG-019：ADR 0039 已接受；ready 参考图支持点击/Enter/Space 打开完整大图预览，
  Escape/显式按钮关闭，拖拽结束不误触。删除控件为 16 × 16 px 且保持独立事件边界。
  最终门禁 239 项（233 通过、6 跳过）；3010 Chrome 已验证 900 × 1199 原图在
  1146 × 772 边界内完整显示、拖拽不误开、控制台无错误，未点击生成。
- GG-020：ADR 0040 已接受并取代 GG-019 的只读预览约束；左侧快捷编辑器使用浏览器
  Canvas，编辑结果作为新素材替换当前参考图，bbox 只输出坐标。所有者鉴权同源读取、
  新素材上传、项目快照同步及刷新恢复均已验证；取消二次确认现使用不透明白色卡片、
  轻遮罩和宫墙红危险操作，并在 3010 复核；最终门禁 245 项（239 通过、6 跳过）。
- GG-021：ADR 0041 已接受；Nano Banana Pro 的三档单张标准报价固定为 15 积分，
  生成路由仍保持关闭。迁移 0019、最终门禁和 3010 API/Chrome 报价核验均已通过。
- Next action: 完成 GG-003 的完整门禁与提交；从最新 `origin/main` 形成精确候选并等待
  CI 发布不可变 GHCR 摘要；随后在生产维护窗口中备份、停旧 Worker、应用 0013～0019、
  验证候选 Web/单 Worker/队列/积分不变量，写入新鲜 alpha 证据并通过门禁后切流。
- Blockers: 当前无代码阻塞。生产切流前仍需 CI 摘要、主机只读基线和 alpha 的新鲜
  精确候选证据；任何一项失败即保持维护并停止。完整删除、举报、外部删除条款与付费
  要求留在搁置项，不自动恢复。

## Milestones

| 阶段 | 状态 | 当前含义 |
| --- | --- | --- |
| M0–M2 | 已完成基线 | 产品/设计契约、前端与容器/CI 基础 |
| M3–M6 | 已完成核心链路 | 持久任务、身份边界、真实模型路径、积分与资产/项目 |
| M7 | 历史验证完成 | 香港链路与备份/恢复等验证；原主机现已转生产 |
| M8 / C6-3 / C7 | controlled alpha 已开放 | 账户审核、生产数据隔离、核心生图、最小恢复/观测 |
| 完整 C6 / full seed | 搁置 | 删除/举报/进一步配套未部署，见 GG-900/901 |
| M9 | 搁置 | 支付/国内支付宝，见 GG-902 |
| ADR 0025 / GG-002 | 已上线 | Nano Banana 2 全 14 宽高比 × 三档分辨率 |
| GG-001 | 已完成（不需部署） | 项目记忆、历史精简、分支隔离与交接协议 |
| GG-004 | 待验收（本地已验证） | 重复派发、同 Worker 重入与失败/完成终态竞态修复 |
| GG-005 | 待验收（本地已验证） | `1K / 2K / 4K` 展示与每张 Asset 实际像素尺寸 |
| GG-006 | 待验收（本地已验证） | 放大的 1:1 参考图预览与比例优先布局 |
| GG-007 | 待实测（本地已验证） | GPT IMAGE 2 SD 精确尺寸、参考图与三档 10 积分 |
| GG-008 | 待实测（3010 已就绪） | 快速重复提交、独立任务状态与逐任务失败恢复 |
| GG-009 | 待实测（3010 已就绪） | GPT IMAGE 2 的 1/2/4 张、10/20/40 积分与多 Asset 原子批次 |
| GG-010 | 待实测（3010 已就绪） | Nano Banana 2 的 1/2/4 张编排、增量任务证据与批次计费 |
| GG-011 | 已完成（无需部署） | 快速小步交付、单次完整门禁与真实 provider 测试隔离 |
| GG-012 | 待实测（3010 已就绪） | Nano 思考程度、Google Search grounding 与文本/图片响应模态 |
| GG-013 | 待站长验收（3010 Web 已就绪） | 稳定生成槽位、具体像素悬浮信息与本地保存下载 |
| GG-014 | 待站长验收（3010 Web 已就绪） | 侧栏积分成功态只保留余额标题与数值 |
| GG-015 | 待实测（3010 已就绪） | GPT IMAGE 2 质量、背景、输出格式及透明兼容联动 |
| GG-016 | 待实测（3010 已就绪） | Nano 隐藏并固定高思考，谷歌搜索保持可选 |
| GG-017 | 待站长验收（3010 已就绪） | 上传参考图持久复用、资产库素材板块与创作素材选择器 |
| GG-018 | 待站长验收（3010 已就绪） | 参考图可见连续序号、拖拽与键盘排序 |
| GG-019 | 待站长验收（3010 已就绪） | ready 参考图完整大图预览与精简删除按钮 |
| GG-020 | 待站长验收（3010 已就绪） | 非破坏性参考图快捷编辑器与 bbox 坐标输出 |
| GG-021 | 待站长验收（3010 已就绪） | Nano Banana Pro 的 `1K / 2K / 4K` 单张 15 积分报价；不开放生成路由 |

## New-session recovery

1. 读根 AGENTS、CURRENT_STATE、[WORKFLOW](WORKFLOW.md)、本文和
   [BACKLOG](BACKLOG.md)，检查 Git 分支/worktree/未提交改动。
2. 有明确新需求就创建新任务卡并读相关专题；“继续”匹配当前任务的下一步。
3. 不要从旧 C6 或归档末尾猜下一阶段；不把 `main` 最新提交当作线上已部署。
4. 本地开发验证后形成干净候选，经明确范围批准再发布。
5. `staging-goodgood.o1key.com` 非现行测试入口；本机为日常测试环境。
   现有生产主机/候选槽验证不是生产数据隔离的独立 staging。

## History and update policy

- [提炼后的历史经验](history/2026-09-07-development-lessons.md)。
- [完整原始开发日志](history/2026-09-07-implementation-log.md)：仅需追溯时读取。
- [搁置源码恢复卡](tasks/GG-900-deferred-c6.md)。
- 本文保持短小、一个检查点/下一步；细节写任务卡，发布事实写 CURRENT_STATE。
  不再追加每次命令结果、所有旧阻塞或聊天记录。
