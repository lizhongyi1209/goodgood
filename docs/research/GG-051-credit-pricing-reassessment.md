# GG-051 图片与视频积分定价评估

日期：2026-09-13。状态：讨论建议，非已接受定价；实际 O1Key 成本尚未提供。

## 现状

- [GG-037](../tasks/GG-037-mixed-media-style-preview.md) 是已确认的混排样式；GG-037 工作树承载后续候选。视频目前为本地预览，并无正式积分、持久任务或资产结算。
- [ADR 0006](../decisions/0006-product-billing-and-provider-routing.md) 已将 GoodGood 售价与上游成本分开。
- [ADR 0009](../decisions/0009-banana-2-flat-credit-price.md) 接受 10 元/500 积分（每积分基准 0.02 元）、Nano Banana 2 每张 10 积分；历史特殊线路成本 0.05—0.10 元/张不是今日核实成本。
- 当前候选 GPT 三模型每张 10 积分；Nano Banana Pro 报价 15 积分但生成线路未开放。图片现价不因本研究改变。
- `server/billing/policy.mjs` 使用精确整数。`server/billing/repository.mjs` 按模型、分辨率、数量与 planContext 查询不可变价格；现有预留关闭为一次全额 settle 或 release。
- 视频 adapter 透传上游响应，但本地 preview 没有经过验证的 usage 成本归一化/持久对账。不能把“接口返回了视频”视为具备计费证据。

## 官方平台的公开做法

下表是查询当日官方页面可见规则；各平台积分单位不等值，不能比较积分数字大小来判断价格。

| 平台与层级 | 公开例子 | 对 GoodGood 的启示 |
| --- | --- | --- |
| Runway 创作产品 | Gen-4.5 每秒 12 积分，5/10 秒为 60/120；图片、视频、音频共用产品积分 | 一个积分单位可承载不同媒体的计价单位 |
| Adobe Firefly 创作功能 | Generate Video 在 24 FPS 下，540p/720p/1080p 分别 20/50/100 积分每秒 | 分辨率和时长可组合成预先可见的价格 |
| Luma Dream Machine | Ray2 Flash 720p 的 5/10 秒为 55/110 积分；Ray2 720p 为 160/320 | 模型与规格档位构成价目表 |
| BytePlus ModelArk API | 视频按 token 单价与消耗估算，官方也给出典型规格的每条/每秒成本例子 | 上游 token 与前台按秒报价可以转换，但应识别输入和最低消耗 |

来源：[Runway](https://help.runwayml.com/hc/en-us/articles/15124877443219-How-do-credits-work)、[Adobe](https://helpx.adobe.com/creative-cloud/apps/generative-ai/generative-credits-faq.html)、[Luma](https://lumalabs.ai/learning-hub/dream-machine-credit-system)、[BytePlus](https://docs.byteplus.com/docs/ModelArk/1099320)。BytePlus 页面打开后重定向且正文未完整展示，本轮公式依据其官方页面的搜索索引正文；不能代替 O1Key 合同或账户价格核对。

Runway 提供月度额度和追加积分，且其网页产品与 API 积分分开。Luma 同样将产品与 API 分开。订阅是积分发放方式，不能替代单次消费成本定价；GoodGood 本轮不因此引入订阅或无限量套餐。

## token 能否换算为明确报价

BytePlus 公开估算涉及 `(输入视频时长 + 输出视频时长) × 输出宽 × 输出高 × 输出帧率 / 1024`，并提示部分参考视频任务有最低 token 消耗，实际以返回的 `usage.completion_tokens` 为准。模型、版本与模式需分别核实；不把此公式直接套用到 O1Key 的 Seedance 2.5 或 HC 线路。

这支持一个有条件的推论：固定规格、输入成本可控时，可以将 token 估算转成固定积分报价；但仅用输出秒数会遗漏参考视频、最低消耗、音频或其他模型相关收费项。视频 token 也不能按提示词字数理解。

## 建议方案

保留单一积分单位及现有兑换基准；个人和企业仍按原有归属分别记账。图片按张；视频按模型/明确线路、分辨率、音频模式、输出时长和实际计费输入计算总积分。数量与提示词分段应逐任务报价后求和；不同规格不能简单乘同一个单价。

优先采用提交前固定报价：服务端根据已验证成本规则计算，冻结该报价及版本，成功交付后收取原报价，明确失败释放。标准/备用分别核算，切换应重新报价，遵守 ADR 0049 不自动切线路。每个独立视频任务单独预留/关闭；多条视频部分失败时，仅成功交付任务扣费。此为待实施建议，不改变现有图片整批结算语义。

如某种模式无法可靠预估，才引入“预计 X，最多 Y”与上限预留，完成后按已披露规则结算、退还差额；不得静默超过用户同意的上限。缺失 usage 不视为零消耗，应进入明确的待对账策略。当前账本不直接支持这种部分结算，需要个人、企业成员预算、来源余额与幂等闭合一起设计。因此不建议第一阶段所有生成都实行事后 token 浮动扣费。

## 如何从成本推导积分

按规格计算完整交付成本 C：实际上游净支出、存储/交付成本，以及失败未退费、必要重试等损耗分摊。存在多类 token 时分别乘对应单价再相加；不得随意把不同价格的输入/输出 token 合成一个 total_tokens 单价。

令 v 为平台每发出一个可消费积分的实际收入，g 为此成本口径下的目标贡献毛利率：

`最低积分报价 = ceil(C / (v × (1 − g)))`

如果手续费按营收比例收取，应从收入端扣除或在公式中单列，避免漏算/重复计算。最终售价还需经用户价值与竞品可比人民币成本验证；成本公式给出底线，不保证市场接受。

仅作算例：假设某条视频完整成本 1.20 元，v=0.02 元，目标毛利率 40%，则报价 100 积分，收入 2 元。若分销/赠送使 v 降为 0.016 元，则同样目标需 125 积分；仍卖 100 积分时毛利率只有 25%。这些数值不是 O1Key 报价，也不是获确认目标。

保留 1 积分=基准 0.02 元足以覆盖视频，无需为 token 改成小数积分。后台用精确货币小单位保存成本，按已公布规则在每个独立任务总价处取整。未来更细粒度业务确有需要时再讨论单位变化。

定价评估应分模型和线路核对全范围成本，尤其 4K、长视频、视频参考和最深销售折扣。不能只用平均用户行为让高成本规格长期亏损。欢迎积分、促销赠送和下游折扣应纳入收入/获客成本模型；不擅自降低现有积分购买力或增加过期限制。

## 下一步所需数据

1. O1Key 标准/备用每个精确模型的实际单价、币种/折扣，计费量字段与查询方式。
2. 文生、图片参考、视频/音频参考的收费区别，分辨率/比例/帧率/时长与最低计费规则，失败/取消/重试费用。
3. 允许的充值赠送和分销折扣、目标利润口径、可接受的报价估算误差。

收到后先产出模型×规格的成本与积分表，覆盖最低实际收入场景。方案确认后才新增 ADR、实施视频报价/预留/结算/对账；本轮不修改代码、余额、旧价或生产状态。
